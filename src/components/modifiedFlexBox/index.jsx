import { styled } from "@mui/system";
import { Box } from "@mui/material";

const ModifiedFlexBox = styled(Box)({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
});

export default ModifiedFlexBox;